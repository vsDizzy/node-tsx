## Minimal example of integrating `.tsx` support to `node.js` esm runtime.

It just prints:

```html
<div>Hello world</div>
```

Install dependencies:

```
pnpm i
```

Start example:

```
pnpm start
```

Start example in watch mode:

```
pnpm dev
```
